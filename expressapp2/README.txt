use this in node command promp to run the app :
1. npm install
2. node server

try to set NODE_ENV to development and production to see the different
# development environment
1. set NODE_ENV=development
2. npm install
3. node server

# development production
1. set NODE_ENV=production
2. npm install
3. node server
